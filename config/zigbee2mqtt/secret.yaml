# DEPRECATED: MQTT password is now injected via env var
# (ZIGBEE2MQTT_CONFIG_MQTT_PASSWORD, forwarded from MQTT_PASS_HEMS_Z2M in .env).
#
# This file is kept only so legacy configuration.yaml entries with
# `!secret mqtt_password` do not break. The env var always wins.
#
# To generate MQTT credentials:
#   bash infra/mosquitto/setup-users.sh --gen
# Then add MQTT_PASS_HEMS_Z2M=... to .env (see env.example).

mqtt_password: ""
