# Linting & formatting
ruff>=0.8.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
coverage[toml]>=7.0.0

# Security scanning
pip-audit>=2.7.0

# Test dependencies (used by test fixtures and test files)
fastapi>=0.115.5
httpx>=0.28.0
websockets>=14.0
aiohttp>=3.13.0

# Adaptive thresholds / drift detection (Phase 2)
river>=0.21,<1

# Bridge-specific test dependencies
# (knowledge-bridge: document processing)
watchdog>=6.0.0
pdfplumber>=0.11.0
python-docx>=1.1.0
beautifulsoup4>=4.12.0
rank-bm25>=0.2.2
# (obsidian-bridge: file watching)
# watchdog already above
# (news-bridge: feed parsing)
feedparser>=6.0.11
