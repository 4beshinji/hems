# Common deps inherited from hems-base:py3.11
# (fastapi, uvicorn, aiohttp, paho-mqtt, pydantic, python-dotenv, loguru)

# STT-specific (speech-to-text & audio processing)
python-multipart>=0.0.20

# Audio processing
soundfile>=0.12.0
numpy>=1.26.0

# STT providers — whisper (default)
faster-whisper>=1.1.0

# ROCm / transformers backend (auto-selected when ROCm detected)
# Also used by qwen3-asr provider
transformers>=4.45.0

# sherpa-onnx provider (uncomment to enable)
# sherpa-onnx>=1.10.0

# Qwen3-ASR provider (uncomment to enable)
# qwen-asr>=0.3.0
