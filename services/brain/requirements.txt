# Common deps inherited from hems-base:py3.11
# (fastapi, uvicorn, aiohttp, paho-mqtt, pydantic, python-dotenv, loguru)

# Database (Event Store)
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
aiosqlite>=0.20.0

# Brain-specific
anthropic>=0.49.0
pyyaml>=6.0.2

# Adaptive thresholds / drift detection (Phase 2)
river>=0.21,<1
