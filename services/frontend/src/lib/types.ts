// ─── Re-export barrel ─────────────────────────────────────────────────────────
// This file is kept as a backwards-compatible re-export shim.
// All type definitions now live in src/lib/types/ (domain-split modules).
// Existing imports from './types' or '@/lib/types' continue to work unchanged.
export * from './types/index'
