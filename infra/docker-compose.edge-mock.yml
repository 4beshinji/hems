services:
  virtual-edge:
    build: ./virtual_edge
    container_name: hems-virtual-edge
    restart: unless-stopped
    environment:
      - MQTT_BROKER=mosquitto
      - MQTT_PORT=1883
      - MQTT_USER=${MQTT_USER:-hems}
      - MQTT_PASS=${MQTT_PASS:-hems_dev_mqtt}
    volumes:
      - ./virtual_edge/src:/app
      - ../edge/lib:/edge_lib
    networks:
      - hems-net

  mock-llm:
    build: ./mock_llm
    container_name: hems-mock-llm
    restart: unless-stopped
    ports:
      - "8001:8000" # Expose on 8001 to avoid conflict if needed, or internal only
    networks:
      - hems-net

  virtual-camera:
    build: ./virtual_camera
    container_name: hems-virtual-camera
    restart: unless-stopped
    ports:
      - "8554:8554"
    networks:
      - hems-net

networks:
  hems-net:
    external: true
    name: infra_hems-net
